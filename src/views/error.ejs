<!DOCTYPE html>
<html>
  <%- include("templates/header") %>
  <body class="bg-gray-50 min-h-screen">
    <%- include("templates/navbar") %>

    <main class="container mx-auto px-4 py-8 flex items-center justify-center min-h-[calc(100vh-120px)]">
      <div class="max-w-md w-full">
        <!-- Ícone de erro -->
        <div class="text-center mb-8">
          <div class="mx-auto w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mb-4">
            <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.732-.833-2.5 0L4.268 6.5c-.77.833-.192 2.5 1.732 2.5z"/>
            </svg>
          </div>
          <h1 class="text-3xl font-bold text-gray-900 mb-2">
            Ops! Algo deu errado
          </h1>
          <p class="text-gray-600 mb-6">
            Encontramos um problema inesperado.
          </p>
        </div>

        <!-- Card do erro -->
        <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
          <div class="mb-4">
            <h2 class="text-lg font-semibold text-gray-900 mb-2 flex items-center">
              <svg class="w-5 h-5 text-red-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              Detalhes do Erro
            </h2>

            <% if (error.message) { %>
              <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-4">
                <h3 class="text-sm font-medium text-red-800 mb-1">Mensagem:</h3>
                <p class="text-sm text-red-700"><%= error.message %></p>
              </div>
            <% } %>

            <% if (error.status || error.statusCode) { %>
              <div class="bg-gray-50 border-l-4 border-gray-400 p-4 mb-4">
                <h3 class="text-sm font-medium text-gray-800 mb-1">Código:</h3>
                <p class="text-sm text-gray-700"><%= error.status || error.statusCode %></p>
              </div>
            <% } %>

            <!-- Detalhes técnicos (colapsível) -->
            <details class="mt-4">
              <summary class="cursor-pointer text-sm font-medium text-gray-700 hover:text-gray-900 transition-colors">
                Ver detalhes técnicos
              </summary>
              <div class="mt-3 p-3 bg-gray-100 rounded-md">
                <pre class="text-xs text-gray-600 whitespace-pre-wrap overflow-x-auto"><%= JSON.stringify(error, null, 2) %></pre>
              </div>
            </details>
          </div>
        </div>

        <!-- Botões de ação -->
        <div class="flex flex-col sm:flex-row gap-3">
          <button
            onclick="window.history.back()"
            class="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-4 rounded-lg transition-colors duration-200 flex items-center justify-center"
          >
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
            </svg>
            Voltar
          </button>

          <a
            href="/"
            class="flex-1 bg-gray-600 hover:bg-gray-700 text-white font-medium py-3 px-4 rounded-lg transition-colors duration-200 flex items-center justify-center"
          >
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
            Página Inicial
          </a>
        </div>

        <!-- Informações adicionais -->
        <div class="mt-6 text-center">
          <p class="text-sm text-gray-500">
            Se o problema persistir, entre em contato com nosso suporte.
          </p>
        </div>
      </div>
    </main>

    <%- include("templates/footer") %>
  </body>
</html>
